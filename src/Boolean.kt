fun main(args: Array<String>) {
    var varBool : Boolean = true
    if(varBool)
        println("varBool bernilai " + varBool)
    var varInt : Int = 1
    if(varInt) //tidak bisa dijalankan karena if hanya bisa digunakan untuk boolean dan tidak dengan yang lainnya
        println("varInt bernilai " + varInt)
}